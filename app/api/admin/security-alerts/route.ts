import { adminApiErrorResponse, requireAdmin } from '@/lib/supabase/admin';
export const runtime='nodejs'; export const dynamic='force-dynamic';
export async function GET(request:Request){try{const {client}=await requireAdmin(request); const p=new URL(request.url).searchParams; let q=client.from('security_alerts').select('id,store_id,alert_type,severity,occurred_at,active_count,registered_count,resolved,resolved_at,resolution_note').order('occurred_at',{ascending:false}); if(p.get('unresolved')==='true') q=q.eq('resolved',false); const {data,error}=await q; if(error)throw error; return Response.json({alerts:data??[]});}catch(e){return adminApiErrorResponse(e);}}
export async function PATCH(request:Request){try{const body=await request.json() as {id?:string;resolutionNote?:string;resolved?:boolean}; const {client,adminUserId}=await requireAdmin(request); if(!body.id)return Response.json({error:'idが必要です'},{status:400}); const {data,error}=await client.from('security_alerts').update({resolved:body.resolved!==false,resolved_at:new Date().toISOString(),resolved_by:adminUserId,resolution_note:(body.resolutionNote||'').slice(0,500)}).eq('id',body.id).select('id,resolved,resolved_at,resolution_note').single(); if(error)throw error; return Response.json({alert:data});}catch(e){return adminApiErrorResponse(e);}}
